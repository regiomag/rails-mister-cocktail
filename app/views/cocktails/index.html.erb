<div class="container">
  <div class="row">
    <% @cocktails.each do |cocktail| %>
      <div class="col-xs-12 col-sm-3">
        <div class="card">
          <%= render 'cocktailCard', cocktail: cocktail%>
        </div>
        <%= link_to cocktail_path(cocktail), {class: "btn btn-primary btn-block"} do %>
          <%= cocktail.name %>
          <span class="label label-primary"></span>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
